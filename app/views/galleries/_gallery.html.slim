b = gallery.name
- if gallery.image_processed?
  = link_to image_tag(gallery.image_url(:mini).to_s), request.parameters.merge(gallery: gallery)
- else
  em Processing...
= link_to "Edit", edit_gallery_path(gallery)
= link_to "Delete", gallery_path(gallery), method: :delete
